- name: uninstalling NGINX
  yum: 
   name: 
    - nginx
   state: absent

- name: Installing httpd
  yum: 
   name: 
    - httpd
   state: present

- name: Starting nginx
  systemd:
   name: httpd
   state: started
   enabled: true
 
- name: deleting old contect
  file:
    path: /usr/share/nginx/
    state: absent
  become: yes

